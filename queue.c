#include "monty.h"

/**
 * set_queue - Set the data structure to quue mode.
 * @stack: Double pointer to the stack.
 * @line_number: Line number.
 * @data_format: Pointer to the data format
 * Return: void
 */

void set_queue(stack_t **stack, unsigned int line_number, int *data_format)
{
	(void)stack;
	(void)line_number;
	*data_format = 0;
}
